<Window x:Class="RestaurantSystem.EmployeesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="СОТРУДНИКИ" Height="600" Width="900" WindowStartupLocation="CenterScreen" Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#CCCCCC" Padding="10">
            <TextBlock Text="СОТРУДНИКИ" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="200" Height="25" Margin="0,0,10,0"/>
            <TextBlock Text="Поиск по ФИО, телефону, должности" VerticalAlignment="Center" Foreground="Gray" FontSize="11"/>
        </StackPanel>

        <DataGrid Grid.Row="2" AutoGenerateColumns="False" Margin="0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="50"/>
                <DataGridTextColumn Header="ФИО" Width="*"/>
                <DataGridTextColumn Header="Должность" Width="120"/>
                <DataGridTextColumn Header="Телефон" Width="120"/>
                <DataGridTextColumn Header="Дата найма" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Content="Добавить" Width="100" Height="30" Background="#555555" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Изменить" Width="100" Height="30" Background="#555555" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Удалить" Width="100" Height="30" Background="#555555" Foreground="White" Margin="0,0,5,0"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Назад" Width="80" Height="30" Background="#555555" Foreground="White" HorizontalAlignment="Right"/>
    </Grid>
</Window>